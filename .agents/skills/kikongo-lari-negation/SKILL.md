---
name: kikongo-lari-negation
description: Rules for negation in Kikongo Lari (ka/ku ... a ... ko), including the tense-dependent 2nd person singular forms (ka wa in past, ku in future/modal). Use when writing, correcting, or reviewing negative verb forms, conjugation tables, translator output, or lesson content in Lari.
---

# Negation in Kikongo Lari

## Core pattern

Verbal negation is discontinuous: **negative marker + (subject particle) + verb + `ko`**, with an `a` linking vowel in the past/modal forms.

## 2nd person singular is tense-dependent — `ku` is NOT systematic

- **Past**: `ka wa ... a ko`
  - `ka wa zaba a ko` = tu ne savais pas / tu ne connaissais pas
- **Future**: `ku ... a ko`
  - `mbaji ku nzaba a ko` = demain tu ne sauras pas
- **Modal (pouvoir)**: `ku lendi ... ko`
  - `ku lendi a dia ko` = tu ne peux pas manger

Never normalize `ku` into `ka wa` or vice versa — both are correct, in different tenses. Attested repeatedly in the translator and user-provided corpus.

## Full paradigms (attested)

Past (savoir/connaître):
```
ka na zaba a ko      = je ne savais pas
ka wa zaba a ko      = tu ne savais pas
ka zaba a ko         = il/elle ne savait pas
ka ta zaba a ko      = nous ne savions pas
ka lua zaba a ko     = vous ne saviez pas
ka ba zaba a ko      = ils/elles ne savaient pas
```

Modal (pouvoir + infinitive):
```
ka ndendi a dia ko    = je ne peux pas manger
ku lendi a dia ko     = tu ne peux pas manger
ka lendi a dia ko     = il/elle ne peut pas manger
ka tu lendi a dia ko  = nous ne pouvons pas manger
ka lu lendi a dia ko  = vous ne pouvez pas manger
ka ba lendi a dia ko  = ils/elles ne peuvent pas manger
```

## Nominal negation

`Ka ... ko` surrounds the nominal predicate, with no `a` linker:

```
Ka muntu ko   = ce n'est pas une personne
```

## Rules to apply

1. Choose the 2SG negative marker by tense: `ka wa` (past), `ku` (future, modal).
2. Keep the closing `ko`; it is never optional.
3. Keep the `a` linker in verbal negatives that carry it.
4. Never invent negative forms. Use only forms from the Jacquot & Lumwamu corpus or supplied by the user.
5. When user-supplied forms look irregular, keep them verbatim — do not normalize.
